import { IExecSyncResult } from 'azure-pipelines-task-lib/toolrunner';
export interface NameValuePair {
    name: string;
    value: string;
}
export declare class Helm {
    private helmPath;
    private namespace;
    private isHelmV3;
    constructor(kubectlPath: string, namespace?: string);
    private setHelmVersion;
    template(releaseName: string, chartPath: string, overrideFiles: string[], overrideValues: NameValuePair[]): IExecSyncResult;
    private _buildSetArguments;
}
