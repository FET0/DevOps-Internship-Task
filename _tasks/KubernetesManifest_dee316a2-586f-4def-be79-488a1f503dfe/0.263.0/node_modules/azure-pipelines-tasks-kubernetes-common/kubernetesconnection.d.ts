export declare class KubernetesConnection {
    ignoreSSLErrors: boolean;
    private kubernetesServiceConnection;
    private tempDir;
    constructor(kubernetesServiceConnection: string, tempDir: string);
    open(): void;
    close(): void;
}
